# Public review build — Omani Dishdasha AI

Generated output. **Do not edit by hand and do not develop on this branch.**

Built from `claude/omani-dishdasha-ai-mvp-9ixppw` by
`.github/workflows/pages-review.yml`, using the standalone single-file export
(`scripts/build-web-artifact.js`): the JS bundle and every asset it reaches for
are inlined, so the page makes zero external requests and works from any
sub-path.

Running in demo mode — seeded catalogue, local stylist, simulated previews,
simulated payments. No credentials of any kind are present in the bundle.

Source of truth for the app is `dishdasha/` on the MVP branch. The tree shown
to the external reviewer is tagged by the `pre-external-audit-v1` branch.
