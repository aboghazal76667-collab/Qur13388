# Copy to server/.env and fill in. This file is the ONLY place a key lives —
# never put one in the React Native app, where it ships to every device.
ANTHROPIC_API_KEY=sk-ant-...

# Optional
PORT=8787
ANTHROPIC_MODEL=claude-sonnet-5
